# Flask-smart-expense-tracker
A full-stack Smart Expense Tracker built with Flask and SQLite, featuring secure user authentication, expense &amp; category management (CRUD), and a clean Bootstrap-based UI.
